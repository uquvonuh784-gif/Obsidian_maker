# 🧠 Состояние проекта (Session State)

> **ВНИМАНИЕ ДЛЯ AI:** Этот файл — первое, что ты читаешь при начале нового разговора.  
> Он содержит текущее состояние проекта и точку, с которой нужно продолжить.

---

## 📌 Текущий статус

- **Проект:** Obsidian Maker — модульный плагин-комбайн для Obsidian
- **Последнее обновление:** 2026-03-06, ~23:36
- **Текущая фаза:** Фаза 2 завершена ✅ → Готовы к **тестированию AI чата**

---

## ✅ Что уже сделано

### Фаза 0 — Подготовка ✅
1. Склонирован официальный шаблон `obsidian-sample-plugin` в `Obsidian_maker/`
2. Установлены зависимости (`npm install`)
3. Создан `AI_WORKFLOW.md` — алгоритм работы ИИ-ассистента
4. Создан `INDEX.md` — главный справочник проекта
5. Создан `.agent/workflows/deploy.md` — воркфлоу сборки и деплоя
6. Создан `.agent/skills/SKILL.md` — скилл по созданию UI (Preact + CSS-переменные Obsidian)
7. Составлен полный план разработки

### Фаза 1 — Ядро плагина ✅
1. ✅ **1.1** Переименован плагин → `obsidian-maker` / `Obsidian Maker` (manifest.json, package.json)
2. ✅ **1.2** Установлен Preact, настроен JSX (tsconfig.json, esbuild.config.mjs)
3. ✅ **1.3** Создана структура папок: `src/core/`, `src/features/`, `src/ui/`, `src/utils/`
4. ✅ **1.4** Написана система модулей: `ModuleRegistry` + интерфейс `PluginModule`
5. ✅ **1.5** Переписан `main.ts` — минимальный загрузчик, sample-код удалён
6. ✅ **1.6** Созданы типы (`types.ts`), настройки (`settings.ts`), утилиты (`debounce.ts`)
7. ✅ Сборка проходит без ошибок (`npm run build` → `main.js` 3.3KB)

### Фаза 2 — AI Чат-ассистент (Groq) ✅
1. ✅ **2.1** Обновлены настройки под Groq API (модели, temperature, system prompt)
2. ✅ **2.2** Создан `GroqService` — сервис для Groq API через `requestUrl`
3. ✅ **2.3** Создан `ChatApp.tsx` — Preact-компонент чата (пузырьки, ввод, typing indicator)
4. ✅ **2.4** Создан `AiChatView` — Obsidian ItemView (боковая панель)
5. ✅ **2.5** Создан `AiChatModule` — модуль (команда, ribbon icon, view)
6. ✅ Обновлены стили (`styles.css`) — полный UI чата
7. ✅ Сборка проходит без ошибок (`main.js` 22.5KB)

## 📂 Ключевые файлы

| Файл | Что содержит |
|------|-------------|
| `src/main.ts` | Точка входа: настройки → AiChatModule |
| `src/core/types.ts` | Интерфейсы: Task, ChatMessage, PluginModule |
| `src/core/settings.ts` | Настройки Groq (API key, model, temperature) + SettingTab |
| `src/core/module-registry.ts` | Реестр модулей |
| `src/features/ai-chat/` | **AI чат модуль** (groq-service, ChatApp, view, module) |
| `src/utils/debounce.ts` | debounce, throttle, generateId |
| `styles.css` | Стили чата (пузырьки, typing indicator, анимации) |
| `SESSION.md` (этот файл) | Текущее состояние |

---

## 🎯 Следующий шаг: Тестирование AI чата

1. **Деплой в тестовый Vault** (`/deploy`)
2. **Вписать API ключ Groq** в настройках плагина
3. **Протестировать** отправку сообщений, ответы, ошибки
4. **Доработки** по результатам тестирования

### После тестирования → Фаза 3 (Канбан-доска задач)

---

## 📝 Создание скиллов/воркфлоу по ходу разработки

| Что создать | Когда | Зачем |
|-------------|-------|-------|
| `workflows/lint.md` | **Фаза 1** ✅ (при необходимости) | Запуск ESLint, автоисправление, правила проекта |
| `skills/obsidian-api.md` | **Фаза 2** — перед началом AI чата | Справочник ключевых API: Vault, Workspace, MetadataCache, requestUrl |
| `skills/testing.md` | **Фаза 2** — после первой рабочей фичи | Ручное и автоматическое тестирование плагина |
| `workflows/release.md` | **Перед первым релизом** | Версионирование, GitHub release, публикация в каталог |

---

## 💡 Принятые архитектурные решения

- **UI фреймворк:** Preact (~3KB), монтируется в ItemView и Modal через `render()`
- **Стили:** CSS-переменные Obsidian, префикс классов `om-`
- **Сеть:** Только `requestUrl` (встроенный в Obsidian)
- **Модульность:** Каждая фича в отдельной папке → класс, реализующий `PluginModule`
- **Код:** TypeScript strict, main.ts минимальный
- **Модули:** `ModuleRegistry` загружает/выгружает в порядке регистрации (обратный при выгрузке)

---

> **Инструкция для AI:** После прочтения этого файла перейди к `AI_WORKFLOW.md` и `INDEX.md`, затем предложи пользователю начать с первой незавершённой задачи из списка выше.
